# Factorial: n! for a non-negative integer.
(
  _ ? (i ? @Integer => [i, 0] | @OP.compare | (-1 => true), _ => true) => !"Only non-negative integers, please!",
  0 => 1,
  n => [n, [n, 1] | @subtract | @] | @OP.product
)
