# Check, whether input string or array is a palindrome
(
  n ? @Number => n | @toString | @,
  s ? @String => s | @chars | @,
  []  => "palindrome",
  [_] => "palindrome",
  [_, ...rest, _] ? @ends | @OP.equal => rest | @,
  _   => "not a palindrome"
)
