# Dot product of two equal-length vectors: zip, multiply each pair, sum.
(
  [u, v] ? ([a ? @Vector, b ? @Vector] => [a | @size, b | @size] | @@OP.equal) =>
    [u, v] | @zip |: (pair => pair | @@OP.product) | @@OP.sum,
  x => !{"kind": "argument_error", "origin": "stdlib", "operation": "@vector/dot", "status": 0, "input": x, "expected": ["_ ? ([u ? @vector/Vector, v ? @vector/Vector] => [u | @size, v | @size] | @@OP.equal)"]}
)
