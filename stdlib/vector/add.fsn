# Add two equal-length vectors elementwise: zip and sum each pair.
(
  [u, v] ? ([a ? @Vector, b ? @Vector] => [a | @size, b | @size] | @@OP.equal) =>
    [u, v] | @zip |: (pair => pair | @@OP.sum),
  x => !{"kind": "argument_error", "origin": "stdlib", "operation": "@vector/add", "status": 0, "input": x, "expected": ["_ ? ([u ? @vector/Vector, v ? @vector/Vector] => [u | @size, v | @size] | @@OP.equal)"]}
)
