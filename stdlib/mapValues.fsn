# Apply f to each value of an object, keeping the keys. Input: {"f": fn, "object": obj}.
(
  {"f": f ? @Function, "object": obj ? @Object} => {"f": (k => [k, [obj, k] | @get | f]), "xs": obj | @keys} | @map | @toObject,
  x => !{"kind": "argument_error", "origin": "stdlib", "operation": "@mapValues", "status": 0, "input": x, "expected": ["{\"f\": _ ? @Function, \"object\": _ ? @Object}"]}
)
