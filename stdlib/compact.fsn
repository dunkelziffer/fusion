# Drop null elements from an array. Built on @filter.
(
  xs ? @Array => {"f": (null => false, _ => true), "c": xs} | @filter,
  x => !{"kind": "argument_error", "origin": "stdlib", "operation": "@compact", "status": 0, "input": x, "expected": ["_ ? @Array"]}
)
