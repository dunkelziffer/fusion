# a < b — derives from @OP.compare.
(
  [a, b] => [a, b] | @OP.compare | (-1 => true, _ => false),
  x => !{"kind": "argument_error", "origin": "stdlib", "operation": "@lt", "status": 0, "input": x, "expected": ["[_, _]"]}
)
