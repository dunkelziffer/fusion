(
  0 => [],
  n ? (m ? @Integer => [-1, m] | @lessThan, _ => false) => [...([n, 1] | @subtract | @range), [n, 1] | @subtract],
  x => !{"kind": "argument_error", "origin": "stdlib", "file": "range.fsn", "operation": "range", "status": 0, "input": x | @sanitize, "expected": ["_ ? (m ? @Integer => [-1, m] | @lessThan, _ => false)"]}
)
