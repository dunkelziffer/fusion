(
  0 => [],
  n ? (m ? @Integer => [-1, m] | @lessThan) => [...([n, 1] | @subtract | @), [n, 1] | @subtract],
  x => !{"kind": "argument_error", "origin": "stdlib", "operation": "@range", "status": 0, "input": x, "expected": ["_ ? (m ? @Integer => [-1, m] | @lessThan)"]}
)
