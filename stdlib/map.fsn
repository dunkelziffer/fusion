(
  {"f": _ ? @Function, "xs": []} => [],
  {"f": f ? @Function, "xs": [x, ...rest]} => [x | f, ...({"f": f, "xs": rest} | @)],
  x => !{"kind": "argument_error", "origin": "stdlib", "operation": "@map", "status": 0, "input": x, "expected": ["{\"f\": _ ? @Function, \"xs\": _ ? @Array}"]}
)
