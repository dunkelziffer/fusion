(
  {"f": _, "xs": []} => [],
  {"f": f, "xs": [x, ...rest]} => [x | f, ...({"f": f, "xs": rest} | @map)],
  x => !{"kind": "argument_error", "location": "stdlib", "file": "map.fsn", "operation": "map", "status": "value", "input": x | @sanitize, "expected": ["{\"f\": _, \"xs\": _ ? @Array}"]}
)
